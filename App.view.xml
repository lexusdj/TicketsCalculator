<mvc:View
	controllerName="Calculator.App"
	displayBlock="true"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:l="sap.ui.layout"
	xmlns:core="sap.ui.core"
	xmlns:tnt="sap.tnt"
	xmlns="sap.m">
	<App id="app">
		<Page title="Ticket Calculator">
            <l:BlockLayout background="Light">
                <l:BlockLayoutRow>
                    <l:BlockLayoutCell>
                    <Label text="Importe" labelFor="inputNumber1"/>
                    <Input
                        id="Importe"
                        type="Number"
                        class="sapUiSmallMarginBottom"
                        placeholder="Enter value" />          
                    <Label text="Ticket1" labelFor="inputNumber1"/>
                    <Input
                        id="Ticket1"
                        type="Number"
                        class="sapUiSmallMarginBottom"
                        placeholder="Enter a ticket value" />

                    <Label text="Ticket2" labelFor="inputNumber2"/>
                    <Input
                        id="Ticket2"
                        type="Number"
                        class="sapUiSmallMarginBottom"
                        placeholder="Enter a ticket value" />            
                    <Button
                        id="Calculate"
                        type="Emphasized"
                        icon="sap-icon://simulate"
                        text="Calculate"
                        press="onCalculate"
                        class="sapUiSmallMargin"/>
                    </l:BlockLayoutCell>
				</l:BlockLayoutRow>
		    </l:BlockLayout>
			
		</Page>
	</App>
</mvc:View>
